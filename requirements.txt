python-jobspy>=1.1.80
streamlit>=1.36
pandas>=2.0
requests>=2.31
PyYAML>=6.0
markdown>=3.5
ddgs>=6.0            # metasearch (DDG/Bing/Brave) for the web-search apply-link resolver
beautifulsoup4>=4.12  # parses job cards out of Gmail digest emails (src/scrape_gmail.py)
google-api-python-client>=2.100   # Gmail lane (opt-in, --gmail)
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2
xhtml2pdf>=0.2.16     # resume PDF download (src/resume.py:tailored_resume_pdf) -- pure
                      # Python, no external binary, so it works the same on the VM
python-docx>=1.1.0    # resume .docx download (tailored_resume_docx)
htmldocx>=0.0.6       # converts the resume's HTML+CSS straight to .docx -- 0.0.9 doesn't
                      # exist on PyPI (max release is 0.0.6); the old >=0.0.9 pin was never
                      # actually satisfiable, just unnoticed because it was already installed
rank_bm25>=0.2.2      # ATS scoring: proper term-saturation-aware relevance (src/jd_match.py),
                      # not naive substring counting. Pure Python, no heavy deps.
sentence-transformers>=3.0   # ATS scoring: local semantic similarity (catches paraphrasing
                      # keyword/BM25 matching can't -- "financial crime" vs "fraud/AML").
                      # Pulls in torch; CPU-only inference on a small model, no API/tokens.
pip-system-certs>=4.0 # Windows: Python's bundled cert store doesn't trust whatever's
                      # intercepting HTTPS here (AV/corporate inspection) even though the OS
                      # store does -- without this, sentence-transformers' one-time model
                      # download from huggingface.co fails with CERTIFICATE_VERIFY_FAILED
                      # every time, silently falling back to a neutral score. Makes Python
                      # use the OS trust store instead of certifi's bundled list.
